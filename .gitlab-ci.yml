pages:
  stage: deploy
  image: alpine:3.20
  script:
    - 'echo "Publishing intentionally vulnerable training site (internal use only): ${CI_PAGES_URL}"'
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
